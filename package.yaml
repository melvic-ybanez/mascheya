name: mascheya

dependencies:
  - base >=4.22.0.0

library:
  source-dirs: src

executable:
  main: Main.hs
  source-dirs: app
  dependencies:
    - mascheya

tests:
  mascheya-tests:
    main: Spec.hs
    source-dirs: test
    dependencies:
      - mascheya
      - QuickCheck 
      - tasty
      - tasty-quickcheck